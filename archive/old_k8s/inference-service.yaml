apiVersion: v1
kind: Service
metadata:
  name: inference
  labels:
    app: inference
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: inference
