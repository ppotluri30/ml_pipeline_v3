# requirements.txt


numpy==2.2.6
pandas==2.3.2
pyarrow==20.0.0
# Use slim mlflow client + pyfunc; avoid heavy extras that pull in large transitive builds
mlflow-skinny==3.3.1
psutil==7.0.0
kafka-python[crc32c]==2.2.15
# numba + llvmlite heavy; only needed for statsforecast optional speedups. Keep but allow version used earlier.
numba==0.62.1
statsforecast==2.0.2
utilsforecast==0.2.12
prophet==1.1.7
# Pin matplotlib to a recent wheel (needed indirectly by prophet for plots) to avoid backtracking to source build
matplotlib==3.10.6
boto3==1.34.162
botocore==1.34.162
scikit-learn==1.5.2